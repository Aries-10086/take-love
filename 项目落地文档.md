# 捡爱 · 项目落地文档

> 版本：v0.2  
> 日期：2026-09-16  
> 状态：M1/M2 本地可运行（规则引擎建议已通）  
> 一句话定位：记录情侣的恋爱时刻，并根据记录给出下一次约会建议，用行动巩固感情。

---

## 1. 背景与目标

### 1.1 要解决的问题

情侣日常相处常常「过了就忘」：有美好瞬间却没有沉淀，想约会又不知道做什么，感情经营容易停在口号上。

「捡爱」把回忆变成可持续的闭环：

**记录 → 建议 → 行动 → 再记录**

### 1.2 产品目标

| 目标 | 说明 |
|------|------|
| 主目标 | 让情侣愿意持续记录相处，并真正完成下一次约会 |
| 体验目标 | 30 秒内记完一条；建议具体可执行，不像鸡汤 |
| 情感目标 | 强化「我们一起走过」的共同感，而不是谁付出更多 |

### 1.3 成功标准（MVP 阶段）

以内测 5–10 对情侣、试用 2 周为窗口：

1. **记录率**：平均每周至少记录 2 次
2. **建议采纳率**：生成建议后 ≥ 30% 被采纳为约会待办
3. **闭环完成率**：采纳后 ≥ 40% 完成并回流成新记录
4. **定性反馈**：至少一半用户认为「建议有点像我们」

---

## 2. 范围界定

### 2.1 MVP 必做（P0）

1. 账号登录与双人恋爱空间绑定
2. 恋爱时刻记录（文字 / 心情 / 标签 / 可选照片）
3. 共享时间线浏览与基础筛选
4. 基于历史记录生成 1–3 条下次约会建议
5. 建议可反馈、可采纳为约会待办
6. 约会完成后自动生成一条新记录

### 2.2 明确不做（P0 阶段）

- 即时聊天 / 动态广场
- 恋爱账本 / 积分排行 / 「谁更爱谁」评分
- 复杂地图打卡、商户优惠
- 原生 iOS / Android App（先做 Web）
- 端到端加密完整方案（先做账号隔离与权限）
- 公开分享到社交平台（隐私优先）

### 2.3 后续可做（P1 / P2）

| 优先级 | 功能 |
|--------|------|
| P1 | 纪念日轻提醒、「一年前的今天」、月度小结 |
| P1 | 建议偏好学习（根据反馈优化） |
| P2 | 小程序 / App 壳、回忆卡片导出 |
| P2 | 更细的预算、天气、地理位置辅助建议 |

---

## 3. 用户与核心场景

### 3.1 目标用户

- 已恋爱、希望更好经营感情的情侣
- 愿意用手机做轻度记录，但不想写长日记
- 偶尔不知道「下次约会做什么」

### 3.2 核心用户故事

1. **作为情侣一方**，我想快速记下今天的相处，以便我们之后能一起回看。
2. **作为情侣**，我想根据过往记录得到下一次约会建议，以免每次都要想很久。
3. **作为情侣**，我们完成约会后希望自动沉淀成回忆，形成持续闭环。
4. **作为情侣一方**，我希望有些记录可以仅自己可见，以保护私人感受。

### 3.3 关键用户路径

```text
注册/登录
  → 创建空间 or 输入邀请码加入
  → 写第一条时刻
  → 浏览时间线
  → 获取下次约会建议
  → 采纳为待办
  → 完成约会
  → 自动生成新记录
  → 回看与再建议
```

---

## 4. 信息架构与页面

### 4.1 页面清单（MVP）

| 页面 | 目的 | 关键元素 |
|------|------|----------|
| 登录页 | 进入产品 | 登录方式、简短品牌说明 |
| 空间引导页 | 创建或加入双人空间 | 创建空间 / 输入邀请码 |
| 首页时间线 | 回看我们的故事 | 记录流、筛选、快捷记一条 |
| 新增/编辑记录 | 低摩擦记录 | 一句话、心情、标签、照片、可见性 |
| 记录详情 | 查看单条时刻 | 内容、图片、编辑/删除 |
| 建议页 | 给出下次行动 | 1–3 条建议、理由、反馈、采纳 |
| 约会待办页 | 跟踪下次行动 | 待定/进行中/完成/取消 |
| 我的/设置 | 空间与账号管理 | 邀请码、解绑说明、退出登录 |

### 4.2 首页原则

- 第一眼是「我们的故事」，不是功能仪表盘
- 首屏避免堆指标、排行、营销卡片
- 主操作只有两个：**记一条**、**要建议**

### 4.3 记录表单字段（MVP）

| 字段 | 必填 | 说明 |
|------|------|------|
| content | 是 | 一句话或短段落，建议 ≤ 200 字引导 |
| mood | 是 | 开心 / 平淡 / 疲惫 / 感动 / 惊喜 等 |
| tags | 否 | 吃饭、散步、电影、旅行、在家、运动… |
| photos | 否 | 最多 9 张（MVP 可先限 3 张） |
| happened_at | 否 | 默认当前时间，支持补记 |
| visibility | 是 | `shared`（双方可见）/ `private`（仅自己） |
| want_again | 否 | 想再来一次 / 一般 / 不太想 |

---

## 5. 功能规格

### 5.1 账号与双人空间

**规则：**

- 每个用户可登录独立账号
- 一个恋爱空间最多 2 名成员（MVP）
- 创建空间后生成邀请码（如 6 位，24 小时或长期有效，二选一，建议先长期 + 可重置）
- 另一方输入邀请码加入后，共享该空间下的 shared 记录与约会

**异常：**

- 邀请码无效 / 空间已满 → 明确提示
- 已在其他空间 → MVP 提示需先退出当前空间（简化）

### 5.2 恋爱时刻（Moments）

**支持：**

- 新增、编辑、删除（删除需二次确认）
- 时间线按 `happened_at` 倒序
- 按心情、标签筛选
- private 记录仅作者可见

**验收：**

- 一方发布 shared 记录，另一方刷新后可见
- 一方发布 private 记录，另一方不可见

### 5.3 约会建议（Suggestions）

**输入：**

- 近 N 条（建议 20）shared 记录摘要
- 用户历史反馈（太贵、太远、太累等）
- 可选约束：时长、预算、今天状态（轻松填或不填）

**输出（每条建议）：**

| 字段 | 说明 |
|------|------|
| title | 短标题，如「傍晚江边慢走 + 一份甜品」 |
| detail | 具体怎么做（步骤化，3–5 句） |
| duration | 预计时长，如 1.5–2 小时 |
| budget | 预算区间，如 50–100 元 |
| reason | 为什么适合你们（引用历史偏好） |
| tags | 建议标签，便于统计 |

**交互：**

- 一次展示 1–3 条
- 可点：喜欢 / 不合适（并选原因）
- 可点：就这个 → 生成约会待办
- 可编辑后再采纳

### 5.4 约会待办（Plans）

**状态机：**

```text
proposed（已采纳）
  → completed（完成） → 自动创建一条 moment
  → cancelled（取消）
```

**完成回流规则：**

- 自动创建 shared moment
- 预填标题/内容来自建议，允许用户补一句感受后再提交
- 关联 `source_plan_id` / `source_suggestion_id` 便于追踪闭环

### 5.5 权限与隐私（MVP）

- 所有读写必须校验：当前用户是否属于该 space
- private moment 仅 author_id 可读
- 解绑空间：MVP 提供「退出空间」；数据归属策略见第 10 节

---

## 6. 建议策略设计

### 6.1 双层策略

**V0 规则引擎（无 LLM 也能用）**

示例规则：

1. 近 3 次均为室内 → 优先短户外
2. 多条 `want_again = 想再来` → 推相似活动的小升级版
3. 近 7 天 mood 偏疲惫 → 推低负担（散步、外卖电影夜、在家仪式感）
4. 超过 14 天无「认真吃饭」标签 → 推一顿不用刷手机的晚餐
5. 最近刚做过同类 → 降权，避免重复推

**V1 LLM 增强**

- 将规则初筛后的候选 + 记录摘要交给模型
- 强制输出 JSON，便于落库与展示
- 模型失败时回退 V0 模板
- Prompt 约束：具体、可执行、不油腻、不提监控/评分

### 6.2 反馈标签（用于学习）

- 太贵了
- 太远了
- 今天好累
- 不够特别
- 不适合我们
- 已经做过了

反馈写入 `suggestion_feedbacks`，下次生成时作为负向约束。

### 6.3 建议质量红线

- 禁止空泛文案（如「多沟通」「制造惊喜」而无具体动作）
- 必须包含：做什么 + 大概怎么安排 + 为何适合你们
- 单次生成耗时目标：< 5 秒（含模型）；超时展示规则兜底

---

## 7. 数据模型

### 7.1 实体关系（概念）

```text
User 1 --- N SpaceMember N --- 1 Space
Space 1 --- N Moment
Space 1 --- N Suggestion
Space 1 --- N Plan
User 1 --- N Moment（author）
Suggestion 1 --- 0..1 Plan
Plan 1 --- 0..1 Moment（完成后回流）
```

### 7.2 表结构草案

#### users

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| email / phone / oauth_id | string | 登录标识 |
| name | string | 昵称 |
| avatar_url | string? | 头像 |
| created_at | datetime | 创建时间 |

#### spaces

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| name | string | 空间名，如「我们的捡爱」 |
| invite_code | string | 邀请码，唯一 |
| created_by | uuid | 创建者 |
| created_at | datetime | 创建时间 |

#### space_members

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| space_id | uuid | 空间 |
| user_id | uuid | 用户 |
| role | enum | owner / member |
| joined_at | datetime | 加入时间 |
| unique(space_id, user_id) | | |
| unique 约束：一用户同时仅属一个 active space（MVP） | | |

#### moments

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| space_id | uuid | 所属空间 |
| author_id | uuid | 作者 |
| content | text | 内容 |
| mood | string | 心情 |
| tags | string[] / json | 标签 |
| photos | json | 图片 URL 列表 |
| visibility | enum | shared / private |
| want_again | enum? | yes / normal / no |
| happened_at | datetime | 发生时间 |
| source_plan_id | uuid? | 若由约会完成回流 |
| created_at / updated_at | datetime | |

#### suggestions

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| space_id | uuid | 所属空间 |
| created_by | uuid | 触发者 |
| payload | json | 1–3 条建议内容 |
| model_version | string | rule_v0 / llm_v1 |
| created_at | datetime | |

#### suggestion_feedbacks

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| suggestion_id | uuid | 建议批次或单条 id |
| user_id | uuid | 反馈人 |
| item_index | int | 第几条 |
| action | enum | like / dislike / adopt |
| reason_codes | string[] | 不合适原因 |
| created_at | datetime | |

#### plans

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| space_id | uuid | 所属空间 |
| suggestion_id | uuid? | 来源建议 |
| title | string | 标题 |
| detail | text | 详情 |
| status | enum | proposed / completed / cancelled |
| scheduled_at | datetime? | 计划时间 |
| completed_at | datetime? | 完成时间 |
| created_by | uuid | 创建者 |
| created_at / updated_at | datetime | |

---

## 8. 技术选型（建议）

| 层级 | 选型 | 理由 |
|------|------|------|
| 前端 | Next.js + TypeScript | 快速出 Web，便于演示与迭代 |
| UI | 自建轻量组件 + CSS Variables | 控制情侣向视觉，避免模板感 |
| 后端 | Next.js Route Handlers 或独立 API | MVP 减少部署复杂度 |
| ORM / DB | Prisma + PostgreSQL | 关系清晰，适合双人空间模型 |
| 鉴权 | Auth.js / Clerk / Supabase Auth | 快速登录 |
| 文件存储 | S3 兼容对象存储 | 存照片 |
| AI | 规则引擎 + 可选 LLM API | 可控、可降级 |
| 部署 | Vercel + 托管 Postgres | 适合早期 |

### 8.1 环境变量（规划）

```text
DATABASE_URL=
AUTH_SECRET=
STORAGE_BUCKET=
STORAGE_ACCESS_KEY=
STORAGE_SECRET_KEY=
LLM_API_KEY=          # 可选，无则仅规则引擎
LLM_BASE_URL=         # 可选
```

### 8.2 非功能要求（MVP）

| 项 | 目标 |
|----|------|
| 首屏可用 | 常规网络 < 3s |
| 记录提交 | < 1.5s（不含大图上传） |
| 建议生成 | < 5s，超时走规则兜底 |
| 权限 | 服务端强制校验，不依赖前端隐藏 |
| 兼容 | 现代移动浏览器优先（情侣多用手机） |

---

## 9. 里程碑与排期

### 里程碑 M0：立项对齐（1 天）

- [x] 产品定位与 MVP 范围确认（本文档）
- [x] 命名、语气、基础视觉方向确认
- [x] 技术栈最终确认（Next.js + Prisma + SQLite 本地）

### 里程碑 M1：可记录（约 5–7 天）

- [x] 初始化仓库与数据库
- [x] 登录注册
- [x] 创建 / 加入空间
- [x] Moment CRUD（图片上传留待下一迭代）
- [x] 时间线与筛选
- [x] 权限校验

**验收：** 两人绑定后，一方记录，另一方可见 shared 内容。

### 里程碑 M2：可建议可行动（约 5–7 天）

- [x] 规则引擎 V0
- [x] 建议页与反馈
- [x] 采纳 → Plan
- [x] 完成 Plan → 回流 Moment
- [x] 简易「本周小结」（首页近 7 天记录次数）

**验收：** 完整跑通「记录 → 建议 → 待办 → 完成 → 新记录」。

### 里程碑 M3：内测打磨（约 5–7 天）

- [ ] 接 LLM V1（可开关）
- [ ] 修复高优体验问题
- [ ] 埋点或最简日志（生成、采纳、完成）
- [ ] 5–10 对情侣试用与复盘

**验收：** 达到第 1.3 节成功标准中的 2 项以上，或明确下一版改什么。

---

## 10. 数据与关系变更策略

### 10.1 退出空间（MVP）

- 用户退出后不再访问该空间
- shared 记录仍保留在空间中（另一方继续可见）
- private 记录：随作者退出可选择「保留不可见」或「删除」，MVP 建议：**仅作者可导出后删除**

### 10.2 分手 / 解散空间（P1）

需单独产品决策，候选方案：

1. 双方同意后解散，数据打包分别导出后删除
2. 一方申请，冷静期后解散
3. 空间冻结为只读归档

**P0 不实现自动销毁流程，仅文档预留。**

---

## 11. 埋点与复盘指标

### 11.1 事件（最小集）

| 事件名 | 触发时机 |
|--------|----------|
| space_created / space_joined | 创建或加入空间 |
| moment_created | 新增记录 |
| suggestion_generated | 生成建议 |
| suggestion_feedback | 喜欢/不合适 |
| plan_adopted | 采纳建议 |
| plan_completed | 完成约会 |
| plan_cancelled | 取消约会 |

### 11.2 每周复盘问题

1. 记录是否太麻烦？卡在哪一字段？
2. 建议像不像「我们」？
3. 有没有采纳但不想完成的原因？
4. 哪一个瞬间最想回看？

---

## 12. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 记录动力不足 | 无数据则建议无意义 | 极致降低记录成本；完成约会自动回流 |
| 建议空洞/重复 | 采纳率低 | 规则兜底 + 强制结构化输出 + 反馈学习 |
| 变成情感压力工具 | 反感卸载 | 不做评分排名；语气克制；允许跳过 |
| 隐私顾虑 | 不愿写真实内容 | 支持 private；强调双人空间隔离；谨慎分享能力 |
| LLM 成本/不稳定 | 体验抖动 | 默认规则可用；LLM 作增强且可关 |

---

## 13. 品牌与体验备注

- 产品名：**捡爱**（拾起、收藏相爱的瞬间）
- 气质：温暖、轻盈、可信，不油腻、不监控
- 文案忌讳：催促、「你已经 N 天没记录」、比较双方付出
- 视觉：手机优先；首页以时间线与情感氛围为主，避免后台仪表盘感
- 动效：克制，用于强化「写下/采纳/完成」的仪式感即可

---

## 14. 目录与交付物规划

建议仓库落地后的文档/代码结构：

```text
捡爱/
├── 项目落地文档.md          # 本文档
├── docs/
│   ├── prd-mvp.md           # 可继续拆的细 PRD（可选）
│   ├── data-model.md        # 与 Prisma schema 同步
│   └── api.md               # 接口说明
├── prisma/
│   └── schema.prisma
├── src/
│   ├── app/                 # 页面与路由
│   ├── components/
│   ├── lib/                 # 鉴权、存储、建议引擎
│   └── server/              # 服务端逻辑
└── README.md
```

### 14.1 本文档对应的下一份交付

按优先级：

1. **数据库 Prisma Schema + 初始化项目骨架**
2. **5 个核心页面线框说明（字段级）**
3. **建议引擎 V0 伪代码 / 接口契约**

---

## 15. 立即执行清单（从今天开始）

### Day 1

- [ ] 确认本文档范围无异议
- [ ] 确认登录方式（邮箱 / 微信 / 都要）
- [ ] 确认是否首版就接 LLM，还是先规则引擎

### Day 2–3

- [ ] 初始化 Next.js + Prisma + Postgres
- [ ] 跑通登录与创建/加入空间

### Day 4–7

- [ ] 完成 Moment 与时间线
- [ ] 完成建议 V0 + Plan 闭环

### Week 2

- [ ] 内测 3 对情侣
- [ ] 根据反馈改一版记录表单与建议话术

---

## 16. 决策记录（Open Questions）

| 编号 | 问题 | 候选 | 当前建议 | 状态 |
|------|------|------|----------|------|
| Q1 | 登录方式 | 邮箱魔法链接 / 微信 / 两者 | 先邮箱，便于开发 | 待定 |
| Q2 | 首版是否接 LLM | 仅规则 / 规则+LLM | 先规则，预留 LLM 开关 | 待定 |
| Q3 | 邀请码有效期 | 永久可重置 / 24h | 永久 + 可重置 | 待定 |
| Q4 | 一人多空间 | 允许 / 不允许 | MVP 不允许 | 建议采纳 |
| Q5 | 照片数量 | 3 / 9 | MVP 先 3 | 建议采纳 |

---

## 17. 附录：建议输出 JSON 示例

```json
{
  "suggestions": [
    {
      "title": "下班后江边慢走，再分享一份甜品",
      "detail": "选一个不用排队太久的江边或湖边路径，走 40–60 分钟；路上只聊三件今天的小事。结束后买一份你们都爱的甜品打包，回家吃。",
      "duration": "1.5-2小时",
      "budget": "40-80元",
      "reason": "你们最近两次记录都偏「在家休息」，且有过「散步很舒服」的标记；这条负担低，也容易再次想做。",
      "tags": ["散步", "甜品", "低负担"]
    }
  ]
}
```

---

**文档维护：** 范围变更时先改第 2 节与第 9 节，再改数据模型与排期。  
**下一步：** 确认第 16 节待定项后，即可初始化代码仓库并进入 M1 开发。
```
